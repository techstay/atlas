# Data Scraping

记录各类网页数据抓取/爬虫工具，重点收录 AI 友好的抓取方案。

## Firecrawl

可以本地部署，也可以使用在线 API，免费额度 1000 积分/月，可以抓取 1000 次单页网页。

安装

```sh
npx skills add https://github.com/firecrawl/cli --skill firecrawl
# Install globally with npm
npm install -g firecrawl-cli
```
