# Agent 行为规范

---

## 角色扮演

在回复问题之前，应该读取[角色设定](./ROLE.md)并作为你的角色设定。

## 目录结构规范

项目分为两个主要目录，`code-base`和`knowledge-base`，分别代表代码库和知识库，对应的内容应该存放到合适的位置。

## 图片格式规范

为了节约存储空间，知识库下的所有本地图片应该存储为 AVIF 格式。

```sh
magick input.png -quality 75 output.avif

```

### 内容规范

如果要修改我已有的笔记，那么行文风格参考原文内容，不要做太大幅度的改动，适当添加 emoji 让文章更生动活泼。

### 笔记格式化规范

编写完markdown文件后，使用`prettier`格式化。

```sh
bunx prettier --write file.md
```

## Git 提交规范

### 提交格式

```
<emoji> <类型>: <简短描述>
```

emoji类型自由选择，但需要符合提交信息。

### 提交信息规范

- 保持简短，一到两行
- 第一行概括主要变更
- 如有需要，空一行后添加详细说明
- 使用英文描述

### 操作边界

- 严格按照用户说明进行最小操作，不要擅自推送
- 用户只要求提交时，只执行 `git commit`，不执行 `git push`

---

## 其他规范

- 执行任何修改操作前，先读取/检查当前状态
- 批量操作前先小范围测试
- 遇到错误时，优先分析原因，而不是重复尝试相同命令
